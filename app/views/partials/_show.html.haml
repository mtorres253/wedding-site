.box.transparent_white
  .row
    .large-12.columns
      %h2.articleTitle 
        = "Your RSVP is confirmed."
  .row
    .large-12.columns &nbsp;
  .row
    .large-12.columns
      .alert-box.hide{"data-alert" => "This is an alert"}
        / Your content goes here
        %a.close{href: "#"} ×
      -if @response.coming
        %h3.articleSubTitle We can't wait to see you! Here are your RSVP details:
        .rsvp_inner_box
          %p 
            = rsvp_number_of_guests(@response)
          %p 
            = rsvp_friday_thingie(@response)
          -if rsvp_comments(@response)
            %p
              = rsvp_comments(@response)
      -else
        %p
          %h3.articleSubTitle We're sorry we'll miss you! Let us know if you change your mind.
        .rsvp_inner_box
      %p= link_to rsvp_button(@response), "javascript:void();", {:class => "large button [radius round]", :onclick => "$('#rsvp_content').html('#{escape_javascript(render :partial => 'partials/form')}');"}
  :javascript
    window.animationMap = new WS.AnimationMap('animation-map').init();